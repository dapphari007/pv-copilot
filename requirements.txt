# --- UI ---
streamlit>=1.36

# --- Data / numerics ---
pandas>=2.2
numpy>=1.26
pyarrow>=15.0            # parquet metadata store

# --- Embeddings + vector store (local, offline) ---
sentence-transformers>=3.0
faiss-cpu>=1.8

# --- LLM backend (Groq, OpenAI-compatible) ---
groq>=0.11
python-dotenv>=1.0

# --- Optional LangChain RAG path ---
langchain-core>=0.3
langchain-groq>=0.2

# --- FastAPI backend (main.py) ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- Optional Milvus vector backend (server via Docker on Windows) ---
pymilvus>=2.4

# --- File parsing (uploads) ---
pypdf>=4.2
python-docx>=1.1

# --- Report export ---
reportlab>=4.2          # PDF
openpyxl>=3.1           # Excel

# --- Auth (redo) ---
PyJWT>=2.8
